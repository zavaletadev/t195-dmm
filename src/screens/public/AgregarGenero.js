import React from 'react';
import { Text, View } from 'react-native';

const AgregarGenero = (props) => {
	return (
		<View
			style={{
				flex: 1,
				justifyContent: 'center',
				alignItems: 'center',
			}}
		>
			<Text>AgregarGenero.js</Text>
		</View>
	);
};

export default AgregarGenero;
