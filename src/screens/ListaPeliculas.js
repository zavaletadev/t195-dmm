import React from 'react';
import { Text, View } from 'react-native';

const ListaPeliculas = (props) => {
	return (
		<View>
			<Text>Lista de peliculas por genero</Text>
		</View>
	);
};

export default ListaPeliculas;
