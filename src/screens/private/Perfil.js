import React from 'react';
import { Text, View } from 'react-native';

const Perfil = (props) => {
	return (
		<View
			style={{
				flex: 1,
				justifyContent: 'center',
				alignItems: 'center',
			}}
		>
			<Text>Perfil.js</Text>
		</View>
	);
};

export default Perfil;
