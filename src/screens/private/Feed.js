import React from 'react';
import { Text, View } from 'react-native';

const Feed = (props) => {
	return (
		<View
			style={{
				flex: 1,
				justifyContent: 'center',
				alignItems: 'center',
			}}
		>
			<Text>Feed.js</Text>
		</View>
	);
};

export default Feed;
