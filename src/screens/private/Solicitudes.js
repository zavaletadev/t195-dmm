import React from 'react';
import { Text, View } from 'react-native';

const Solicitudes = (props) => {
	return (
		<View
			style={{
				flex: 1,
				justifyContent: 'center',
				alignItems: 'center',
			}}
		>
			<Text>Solicitudes.js</Text>
		</View>
	);
};

export default Solicitudes;
