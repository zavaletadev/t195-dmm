import React from 'react';
import { Text, View } from 'react-native';

const ListaAdopcion = (props) => {
	return (
		<View
			style={{
				flex: 1,
				justifyContent: 'center',
				alignItems: 'center',
			}}
		>
			<Text>ListaAdopcion.js</Text>
		</View>
	);
};

export default ListaAdopcion;
